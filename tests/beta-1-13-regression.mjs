import fs from "node:fs";import assert from "node:assert/strict";const game=fs.readFileSync(new URL("../game.js",import.meta.url),"utf8"),html=fs.readFileSync(new URL("../index.html",import.meta.url),"utf8");assert.match(game,/GAME_VERSION = "1\.13 BETA"/);assert.match(game,/DAYBREAK: \["#fff7cf", "#ffad62"\]/);assert.match(game,/\[CONSTELLATION_IDS\.DAYBREAK\]: Object\.freeze\(\{\s*createRuntime/);assert.match(game,/count === 1 \? summonSequencePreview\(\[\]\)/);assert.match(game,/pendingResultHtml/);assert.match(game,/selectedHasLinkedConstellation/);assert.match(html,/data-open-profile/);assert.match(html,/data-main-menu-toggle/);assert.match(html,/data-profile-nickname-input/);console.log("1.13 regression assertions passed");